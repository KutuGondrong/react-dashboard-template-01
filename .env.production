# Another-user production only (make build): dev keeps .env default (true) for landing pages + banner
# No VITE_BASE_PATH: template apps deploy at / unless overridden locally
VITE_SHOW_DEV_FEATURES=false
