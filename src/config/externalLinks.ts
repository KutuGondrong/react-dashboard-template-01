import externalLinks from './external-links.json';

export type ExternalLinks = typeof externalLinks;

/** Public URLs for Tutorial & Components — used by template and generated apps. */
export const externalLinksConfig: ExternalLinks = externalLinks;
