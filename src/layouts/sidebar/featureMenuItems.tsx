/**
 * Feature sidebar menu items — edit manually or append via make feature.
 * Merged into useSidebar.tsx via buildFeatureMenuItems().
 */
import type { NavMenuItem } from '@/components/NavMenu';

export function buildFeatureMenuItems(_t: (key: string) => string): NavMenuItem[] {
  return [];
}
